'use strict';

/**
 * @ngdoc function
 * @name stephcurrywiththeshotApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the stephcurrywiththeshotApp
 */
angular.module('stephcurrywiththeshotApp')
  .controller('MainCtrl', function ($scope, Auth, $firebaseArray, Ref) {

    // $scope.games = $firebaseArray(Ref);

  });
